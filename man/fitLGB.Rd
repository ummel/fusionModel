% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitLGB.R
\name{fitLGB}
\alias{fitLGB}
\title{Internal LightGBM Hyperparameter Tuning and Model Fitting}
\usage{
fitLGB(
  dfull,
  dtrain = NULL,
  dvalid = NULL,
  cv.folds = NULL,
  hyper.grid,
  params.obj
)
}
\arguments{
\item{dfull}{An \code{\link[lightgbm]{lgb.Dataset}} object containing the full dataset
(predictor matrix and target outcome) used for the final model fit.}

\item{dtrain}{An optional \code{\link[lightgbm]{lgb.Dataset}} object containing the
training subset when validation-set evaluation is performed. Default is \code{NULL}.}

\item{dvalid}{An optional \code{\link[lightgbm]{lgb.Dataset}} object containing the
validation subset used for early stopping and performance comparison. Default is \code{NULL}.}

\item{cv.folds}{A list of integer vectors specifying predefined fold indices for K-fold
cross-validation (via \code{\link[lightgbm]{lgb.cv}}), or a logical flag (\code{TRUE}) when
a single validation split is used.}

\item{hyper.grid}{A list of lists or data frame/grid where each element represents a
specific hyperparameter combination (e.g., learning rate, tree depth, feature fraction)
to evaluate.}

\item{params.obj}{A named list of global LightGBM parameters, including objective setting,
metric definitions, and thread counts.}
}
\value{
An object of class \code{\link[lightgbm]{lgb.Booster}} containing the final trained model,
augmented with \verb{$best_score}, \verb{$best_iter}, and \verb{$record_evals} metadata summarizing the tuning process.
}
\description{
\code{fitLGB()} is an internal helper function invoked during \code{\link{train}} to perform
hyperparameter grid searches and train final gradient boosting models using the
\pkg{lightgbm} package. It evaluates candidate hyperparameter sets via K-fold
cross-validation or a single validation split, selects the combination minimizing the
loss metric, and returns a final model trained on the complete dataset.
}
\details{
Internal Fitting Routine for LightGBM Models

\strong{Workflow:}
\itemize{
\item \strong{Cross-Validation / Validation Evaluation:} Loops through each candidate row in
\code{hyper.grid}. Depending on whether \code{cv.folds} is a list of fold indices or a logical flag,
it executes \code{\link[lightgbm]{lgb.cv}} or \code{\link[lightgbm]{lgb.train}} with early
stopping enabled (\code{early_stopping_rounds = 2L}).
\item \strong{Optimal Selection:} Compares \code{best_score} across all candidate hyperparameter sets,
identifying the set that achieved the lowest evaluation loss and its associated optimal
number of boosting iterations (\code{best_iter}).
\item \strong{Final Model Training:} Refits the model on the full dataset (\code{dfull}) using the optimal
hyperparameters and fixed iteration count (\code{num_iterations = best_iter}).
\item \strong{Result Augmentation:} Attaches a metadata data.frame containing the grid search
results to the returned \code{lgb.Booster} object's \verb{$record_evals} slot.
}
}
\seealso{
\code{\link{train}}
}
\keyword{internal}
